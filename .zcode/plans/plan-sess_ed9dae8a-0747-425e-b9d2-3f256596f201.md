## Context

Phase 8 (sticky sessions) core implementation already exists in the working tree (uncommitted): `StickySessionConfig`, `StickySessionPool` (decorator over any `BackendPool`), wiring in `ConfigLoader`/`AppConfig`/`LoadBalancerApplication`/`ProxyHandler`/`ProxyResult`, a `sticky_session` block in `config.yaml`, `docs/phase8.md`, and 11 tests in `StickySessionPoolTest`. What's missing to complete Phase 8 per the project's own conventions (phase6.md format, test plan in phase8.md):

## Work items

1. **Add missing tests** (per docs/phase8.md test plan table)
   - `StickySessionPoolTest`: sticky wrapping `LeastConnectionsPool` (not just round-robin); pinned backend at capacity → fallback.
   - `ProxyResultTest`: `Set-Cookie` injection tests — cookie present with correct `Path=/; Max-Age=3600; HttpOnly` when enabled, no cookie when disabled, no cookie when `backendName == null` (error results), session-cookie form when ttl=ZERO.
   - `ConfigLoaderTest`: `sticky_session` parsing — defaults when block absent, custom values honored.
   - `ProxyHandlerTest` (end-to-end with real local HttpServer backends): first response sets cookie; subsequent request with cookie always hits same backend; pinned backend down → fallback + re-pin with new cookie; sticky disabled → no cookie header.
   - `StickySessionConfigTest` (new): `buildCookieHeader` variants (ttl, httpOnly, secure, session cookie).

2. **Add `StickySessionBenchmark`** (JMH, mirroring `PoolBenchmark` style): cookie parse (`parseCookieValue`) seq/concurrent, sticky-hit path vs sticky-miss (delegate) seq/concurrent, wrapped vs plain pool overhead comparison. Use the anonymous-`HttpExchange` dummy pattern with a `Cookie` header.

3. **Run full test suite** (`mvn test`) — fix any failures; ensure all green.

4. **Run benchmarks** via `run-benchmarks.sh` (or JMH filtered to the new benchmark first for speed, then full suite if time permits).

5. **Complete `docs/phase8.md`**: add "JMH Benchmark Results" (tables + analysis), "Test Coverage" table with final counts, and "Failure Scenarios" section — matching phase6.md format.

6. **Update `README.md`**: mark Phase 8 (and 5–7, which are done per commits) in Completed Phases; refresh the project-structure tree (`session/`, `circuit/`, benchmarks, `run-benchmarks.sh`); add a Sticky Sessions section documenting the `sticky_session` config block and cookie format.

7. **Commit** the completed Phase 8 work as a single commit (excluding `target/` churn — I'll check whether `target/` is tracked and leave those files alone or note a `.gitignore` suggestion).

## Notes
- No changes needed to the core sticky-session implementation itself — it matches the phase8.md design; only tests/benchmarks/docs/README remain.
- README roadmap calls sticky sessions "Phase 8" and docs file is `phase8.md` — numbering is consistent; no `phase7.md` exists but that's pre-existing and I won't touch it.